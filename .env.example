# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Authentication
# In this app, we use a RepGate (client-side) for sales rep attribution.
# Ensure the above keys are added to your Vercel Project Settings -> Environment Variables.
